import SidebarLayout from "@/components/SidebarLayout";

export default function FormKeluargaLayout({ children }: { children: React.ReactNode }) {
  return <SidebarLayout>{children}</SidebarLayout>;
}
